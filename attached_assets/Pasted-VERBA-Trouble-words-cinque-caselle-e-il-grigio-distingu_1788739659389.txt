VERBA — Trouble words: cinque caselle, e il grigio distingue due cose diverse.
ECONOMY MODE, Claude Sonnet 5, Effort Low. Stima ~$0.20–0.40.

DO NOT test live — I test manually.
NON toccare attached_assets/.
Applica OGNI edit in modo indipendente: salta e segnala i mismatch senza fermarti.

Il numero degli errori NON cambia: resta il totale storico. Le caselle
raccontano come sta andando adesso, il numero da dove viene.

════════════════════════════════════════════════════════════
1 · src/lib/progressStats.ts
════════════════════════════════════════════════════════════
TROVA:
/** Quanti tentativi mostra la striscia. history[] ne conserva 12. */
const STRIP_LEN = 6;

SOSTITUISCI CON:
/** Quanti tentativi mostra la striscia: gli ultimi cinque incontri. */
const STRIP_LEN = 5;

════════════════════════════════════════════════════════════
2 · src/pages/ProgressScreen.tsx
════════════════════════════════════════════════════════════
TROVA l'intera funzione AttemptStrip, dal commento che la precede fino alla
graffa che la chiude, e SOSTITUISCILA CON:

/** Le caselle della striscia: gli ultimi cinque incontri con la parola. */
const STRIP = 5;

/**
 * Tre stati, non due.
 *   verde  — hai risposto bene
 *   rosso  — hai risposto male
 *   grigio PIENO — l'incontro c'è stato, ma è precedente alla cronologia:
 *                  l'app non sa com'è andata
 *   grigio VUOTO — quell'incontro non è mai avvenuto
 *
 * Senza questa distinzione una parola vista dieci volte mostrava caselle
 * vuote accanto a "✗ 6", e il vuoto sembrava dire "non è successo niente".
 * I grigi pieni spariranno da soli: ogni risposta nuova riempie la storia.
 *
 * Il numero accanto è un'altra misura — gli errori di sempre. Cinque verdi
 * accanto a un 10 vogliono dire che quella parola l'hai battuta.
 */
function AttemptStrip({ attempts, seen }: { attempts: Array<"c" | "e">; seen: number }) {
  // quanti incontri ci sono stati davvero, dentro la finestra delle cinque
  const real = Math.min(STRIP, Math.max(seen, attempts.length));
  const unknown = Math.max(0, real - attempts.length);   // avvenuti, non registrati
  const empty = Math.max(0, STRIP - real);               // mai avvenuti
  const cells: Array<"c" | "e" | "unknown" | "empty"> = [
    ...Array.from({ length: empty }, () => "empty" as const),
    ...Array.from({ length: unknown }, () => "unknown" as const),
    ...attempts,
  ];
  return (
    <span aria-label={`Ultimi ${STRIP} incontri`}
      style={{ display: "flex", gap: 3, alignItems: "center" }}>
      {cells.map((c, i) => (
        <i key={i} style={{
          width: 7, height: 7, borderRadius: 2, display: "block", boxSizing: "border-box",
          background:
            c === "c" ? GREEN :
            c === "e" ? RED :
            c === "unknown" ? "rgba(255,255,255,0.22)" : "transparent",
          border: c === "empty" ? "1px solid rgba(255,255,255,0.13)" : "none",
        }} />
      ))}
    </span>
  );
}

E POI TROVA:
            <AttemptStrip attempts={entry.attempts} />

SOSTITUISCI CON:
            <AttemptStrip attempts={entry.attempts} seen={entry.seen} />

──────────────────────────────── VERIFICA E RIPORTA
· Edit applicati e saltati
· Typecheck: sì/no + errori per intero
· Conferma che `seen` è presente in TroubleEntry e viene popolato (dovrebbe
  esserci già da un intervento precedente); se manca, FERMATI e segnalalo
· Conferma che il numero `entry.wrong` accanto alla striscia è invariato

NON FARE
· Nessun altro file. NON toccare wordStats, QuizScreen, ContextQuestion.
· NON avviare il dev server, NON testare.