VERBA — TroubleRow: la riga diventa premibile, la freccia sparisce.
ECONOMY MODE, Claude Sonnet 5, Effort Low. Stima ~$0.50–0.90. Un file solo.

DO NOT test live — I test manually.
NON toccare attached_assets/. Stringhe simili vivono lì: ignorale.
SOLO edit mirati old_string → new_string. NESSUNA sostituzione integrale di file.
Applica OGNI edit in modo INDIPENDENTE: se una find non combacia ESATTAMENTE,
SALTA quell'edit e SEGNALALO, ma esegui comunque gli altri.
Se combacia il contenuto ma non l'indentazione, applica rispettando
l'indentazione REALE del file e segnalalo nel rapporto.

FILE: src/pages/ProgressScreen.tsx

COSA CAMBIA, in una frase: oggi lo storico di una parola si apre solo premendo
il chevron ">" largo 15px; dopo, si apre premendo la riga in qualunque punto,
tranne la stella. Il chevron viene eliminato.

════════════════════════════════════════════════════════════
1 · LA BANDIERA CHE PROTEGGE LO SWIPE
════════════════════════════════════════════════════════════
La riga si trascina a sinistra per toglierla dall'elenco. Con un onClick sulla
riga, uno swipe che torna indietro finirebbe per aprire lo storico della parola
che stavi buttando via. Serve una bandiera che il click possa interrogare.

TROVA:
import { tapScale, TAP_SPRING } from "@/components/SpringTap";

SOSTITUISCI CON:
import { tapScale, TAP_SPRING } from "@/components/SpringTap";

// Alzata da onDragStart, azzerata a ogni onPointerDown. Serve a impedire che
// la fine di uno swipe apra lo storico. Sta a livello di modulo e non dentro il
// componente di proposito: i gesti del puntatore sono esclusivi, due righe non
// possono essere trascinate insieme, e così l'edit non deve toccare la firma di
// TroubleRow. NON usare un setTimeout dopo onDragEnd: il timer può scattare tra
// il pointerup e il click, e in quel caso lo storico si apre lo stesso.
let dragGuard = false;

════════════════════════════════════════════════════════════
2 · LA RIGA DIVENTA PREMIBILE
════════════════════════════════════════════════════════════
TROVA:
          onDragEnd={(_, info) => { if (info.offset.x < -64) onDismiss(entry.id); }}

SOSTITUISCI CON:
          onPointerDown={() => { dragGuard = false; }}
          onDragStart={() => { dragGuard = true; }}
          onDragEnd={(_, info) => { if (info.offset.x < -64) onDismiss(entry.id); }}
          onClick={() => { if (!dragGuard) onOpen(entry); }}
          role="button"
          tabIndex={0}
          aria-label={`Storico di ${entry.word}`}
          onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onOpen(entry); } }}

════════════════════════════════════════════════════════════
3 · IL CURSORE
════════════════════════════════════════════════════════════
Il gesto primario ora è il tocco, non il trascinamento.

TROVA:
touchAction: "pan-y", cursor: "grab" }}

SOSTITUISCI CON:
touchAction: "pan-y", cursor: "pointer" }}

════════════════════════════════════════════════════════════
4 · VIA IL CHEVRON
════════════════════════════════════════════════════════════
Elimina l'intero bottone. La `aria-label` che portava è già stata spostata
sulla riga all'edit 2, quindi non si perde nulla per chi usa screen reader.

TROVA:
            <motion.button onClick={(e) => { e.stopPropagation(); onOpen(entry); }}
              onPointerDown={(e) => e.stopPropagation()}
              whileTap={tapScale("icon")}
              transition={TAP_SPRING}
              style={{ background: "none", border: "none", cursor: "pointer", padding: "3px 1px", display: "flex", alignItems: "center", color: "rgba(255,255,255,0.3)" }}
              aria-label={`Storico di ${entry.word}`}>
              <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><path d="M9 6l6 6-6 6" /></svg>
            </motion.button>

SOSTITUISCI CON: (niente — stringa vuota, rimuovi il blocco e la riga che resta vuota)

DOPO averlo tolto, controlla se `ChevronRight` è ancora usato altrove nel file.
· Se NON è più usato da nessuna parte → toglilo dalla riga di import di
  lucide-react, lasciando intatti gli altri nomi.
· Se è ancora usato → lascia l'import com'è.
Riporta quale dei due casi era, con i numeri di riga degli usi rimasti.

════════════════════════════════════════════════════════════
5 · LA STELLA — solo verifica, nessuna modifica
════════════════════════════════════════════════════════════
La stella deve restare l'unica esclusione: premendola NON si deve aprire lo
storico. Il suo `onClick` ha già `e.stopPropagation()`, che ferma il click
prima che arrivi alla riga.
NON modificare niente. Copia nel rapporto la riga di apertura del bottone
stella così com'è nel file adesso, e conferma che `e.stopPropagation()` c'è.

════════════════════════════════════════════════════════════
6 · I DUE ⓘ — solo lettura, nessuna modifica
════════════════════════════════════════════════════════════
NON modificare niente. Servono le stringhe vere per il prossimo intervento.

6a) Stampa le righe da 325 a 350 del file (numeri di riga inclusi).
6b) Stampa le righe da 358 a 382 del file (numeri di riga inclusi).
6c) Per ciascuno dei due toggle ⓘ dichiara: il tag di apertura è `<button` o
    `<motion.button`? E la chiusura è `</button>` o `</motion.button>`?
    Se in uno dei due l'apertura e la chiusura NON sono dello stesso tipo,
    dillo subito e per primo: significa che un edit precedente è passato a metà.

════════════════════════════════════════════════════════════
VERIFICA — rispondi VOCE PER VOCE, non in prosa
════════════════════════════════════════════════════════════
a) Conteggio righe di src/pages/ProgressScreen.tsx PRIMA e DOPO.
   Atteso: partenza 518, arrivo tra 520 e 528 (circa +14 di aggiunte, −9 per il
   chevron tolto). Un numero fuori da questa finestra va spiegato.
b) Se il conteggio è RADDOPPIATO o più: FERMATI, non fare altro, dillo subito.
c) Edit 1 bandiera dragGuard: sì/no.
d) Edit 2 props sulla riga: sì/no. Elenca i props che il tag ha ADESSO, in
   ordine, dal primo all'ultimo.
e) Edit 3 cursore: sì/no.
f) Edit 4 chevron rimosso: sì/no. `ChevronRight` tolto dall'import o no, e
   perché (vedi sopra).
g) Punto 5: la riga di apertura della stella, copiata dal file.
h) Punti 6a, 6b, 6c per intero.
i) Typecheck: pulito sì/no, con gli errori riportati per intero se ce ne sono.
j) Elenco degli edit SALTATI e il motivo di ciascuno.
k) Conferma esplicita: dentro la riga esiste ancora ESATTAMENTE UN onClick che
   chiama `onOpen` (quello dell'edit 2) e nessun altro. Se ne trovi due, il
   chevron non è stato rimosso: dillo.

NON FARE
· Nessun altro file. NON toccare SpringTap.tsx, WordHistorySheet.tsx,
  ResultsScreen.tsx, wordStats.ts, attached_assets/.
· NON avviare il dev server, NON testare.
· NON riscrivere il file per intero.
· NON toccare lo swipe: `drag`, `dragConstraints`, `dragElastic` e la soglia
  −64 restano esattamente come sono.